--- bCNC.orig	2018-04-03 12:56:16 UTC
+++ bCNC
@@ -1,10 +1,10 @@
-#!/usr/bin/env sh
+#!/bin/sh
 
-DIR=`dirname $0`
-PYTHONPATH=${DIR}:${DIR}/lib:${DIR}/plugins
+DIR=%%LOCALBASE%%/%%PORTNAME%%
+PYTHONPATH=%%LOCALBASE%%/%%PORTNAME%%:%%LOCALBASE%%/%%PORTNAME%%/lib:%%LOCALBASE%%/%%PORTNAME%%/plugins
 export DIR PYTHONPATH
 if [ .$PYTHON = . ]; then
-	PYTHON=python
+	PYTHON=%%PYTHON_CMD%%
 fi
 ${PYTHON} ${DIR}/bCNC.py $*
 #python -m cProfile -o bCNC.out ${DIR}/bCNC.py $*
